<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title><%= title %></title>
    <style>
        .card {
            margin-bottom: 20px;
        }
        .progress-bar-container {
            position: relative;
            width: 100%;
            height: 40px;
            background-color: #e0e0e0;
            border-radius: 20px;
            overflow: hidden;
            margin-top: 20px;
        }
        .progress-bar {
            position: absolute;
            height: 100%;
            background: linear-gradient(90deg, #007bff 0%, #0056b3 100%);
            color: white;
            text-align: center;
            line-height: 40px;
            border-radius: 20px;
            white-space: nowrap;
            transition: width 1s ease-in-out;
            animation: fillBar 2s forwards;
        }
        .progress-bar span {
            position: relative;
            z-index: 1;
        }
        .container {
            max-width: 1200px;
        }
        .active-domains-container {
            border: 2px solid #007bff;
            border-radius: 10px;
            padding: 15px;
            background-color: #f0f8ff;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .active-domains-header {
            font-size: 1.25rem;
            font-weight: bold;
            color: #007bff;
        }
        .active-domains-count {
            font-size: 2rem;
            font-weight: bold;
            color: #0056b3;
        }
        .progress-bar-container p {
            font-size: 1.125rem;
            font-weight: bold;
            color: #0056b3;
            text-align: center;
            margin-top: 10px;
        }

        @keyframes fillBar {
            from {
                width: 0;
            }
            to {
                width: <%= (totalDomains > 0 ? (activeDomains / totalDomains * 100).toFixed(2) : 0) %>%;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/header', { title: title, pageHeading: 'Welcome to Your Dashboard' }) %>

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header bg-primary text-white">
                        Overview
                    </div>
                    <div class="card-body">
                        <h5 class="card-title">Dashboard Summary</h5>
                        <p class="card-text">Here you can get a quick overview of your domain statuses, notifications, and other key metrics.</p>
                        <a href="/domain-management" class="btn btn-primary">Go to Domain Management</a>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="active-domains-container">
                    <div class="active-domains-header">
                        Active Domains
                    </div>
                    <div class="active-domains-count">
                        <%= activeDomains %>
                    </div>
                </div>
                <div class="progress-bar-container mt-4">
                    <div class="progress-bar" style="width: <%= (totalDomains > 0 ? (activeDomains / totalDomains * 100).toFixed(2) : 0) %>%">
                        <span><%= activeDomains %> / <%= totalDomains %></span>
                    </div>
                </div>
                <p class="text-center mt-3">Active Domains: <%= activeDomains %> / Total Domains: <%= totalDomains %></p>
            </div>
        </div>
    </div>
    
    <!-- Include Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
